# ── Personal data — NEVER commit ────────────────────────────────────────────
# This repo is public. Your profile, search prefs, resume, and application log
# live in the clone on your machine only. The committed *.example.* files are
# the placeholders; your real files below stay out of git.
config/profile.json
config/search.yml
config/resume.*
data/applications.csv
data/*.csv

# Per-machine Claude Code settings (not shipped)
.claude/settings.local.json

# ── Build artifacts / deps ──────────────────────────────────────────────────
node_modules/
/dist/
mcp/src/node_modules/
mcp/src/package-lock.json

# Chrome Web Store publishing credentials — never commit
.env

# ── OS / editors / logs / temp ──────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~
*.log
npm-debug.log*
/tmp/
/.cache/
